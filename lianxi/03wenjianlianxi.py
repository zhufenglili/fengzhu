
# os.path.exists("b")  #判断这个文件是否存在
# if not os.path.exists('b'): #如果目录不存在
#     os.mkdir("b") #创建目录
# if not os.path.exists("b/lili.txt"): #判断这个目录下有没有这个文件
#   f = open("b/lili.txt","w") #如果没有打开文件，写模式
#   f.write("hello") #写入内容
#   f.close() #
import time
print(time.time())